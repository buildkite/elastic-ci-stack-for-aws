#!/bin/bash

set -eu -o pipefail

echo "stopping buildkite-agent gracefully"

service buildkite-agent stop

while [[ $(service buildkite-agent status) == "Running" ]]; do
  sleep 0.5
done

echo "buildkite-agent stopped"
